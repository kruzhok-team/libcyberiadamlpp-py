set(CPACK_VERBATIM_VARIABLES YES)
set(CPACK_PACKAGE_VERSION_MAJOR ${PROJECT_VERSION_MAJOR})
set(CPACK_PACKAGE_VERSION_MINOR ${PROJECT_VERSION_MINOR})
set(CPACK_PACKAGE_VERSION_PATCH ${PROJECT_VERSION_PATCH})
set(CPACK_PACKAGE_VENDOR "Kruzhok Association")
set(CPACK_PACKAGE_CONTACT "aleksey@fedoseev.net")
set(CPACK_RESOURCE_FILE_LICENSE "${CMAKE_CURRENT_SOURCE_DIR}/LICENSE")
set(CPACK_RESOURCE_FILE_README "${CMAKE_CURRENT_SOURCE_DIR}/README.md")

# the python module package
set(CPACK_DEBIAN_FILE_NAME DEB-DEFAULT)
set(CPACK_DEBIAN_PACKAGE_ARCHITECTURE "amd64")
set(CPACK_DEBIAN_PACKAGE_NAME "python3-libcyberiadamlpp")
set(CPACK_DEBIAN_PACKAGE_DESCRIPTION "The Python 3 binding for the Cyberiada state machine graphs processing library")
# libxml2 bumped its soname on newer releases (libxml2.so.2 -> .so.16, package
# renamed libxml2 -> libxml2-16); accept either so the package installs on both
set(CPACK_DEBIAN_PACKAGE_DEPENDS "libxml2 (>= 2.9) | libxml2-16, python3 (>= 3.8), libcyberiadamlpp (>= 1.0.6), libcyberiadaml (>= 1.0.6), libhtreegeom (>= 1.0.6)")

# a per-release build tags the version (e.g. 1.0.6~ubuntu2404) so the binding .deb,
# which is built for this release's Python 3, is identifiable across releases
set(DISTRO_TAG "@DISTRO_TAG@")
if(NOT DISTRO_TAG STREQUAL "")
  set(CPACK_DEBIAN_PACKAGE_VERSION "${CPACK_PACKAGE_VERSION}~${DISTRO_TAG}")
endif()
